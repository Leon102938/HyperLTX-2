Return JSON only. Review what is visibly present in the frames, not just the prompt metadata. Mark visible phones, screens, UI, fake text, readable text, subtitles inside the image, split-screen layouts, collage layouts, repeated panels, logos, labels, documents, or signs. If uncertain, use needs_review. In clean lifestyle modes, never return passed when visible text, device, screen, or UI is present.
